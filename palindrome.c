#include<stdio.h>
int main(){
	int n,r,sum=0,temp;
	scanf("%d",&n);

	while (n>0)
		{
			
		}
	if(temp==sum){
		printf("%d is a palindrome.",temp);
	}
	else
		{
		printf("%d is not a palindrome.",temp); 
		}
return 0;
}
